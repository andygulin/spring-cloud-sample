spring:
  application:
    name: consumer
eureka:
  instance:
    hostname: localhost
    port: 1111
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${eureka.instance.port}/eureka/
